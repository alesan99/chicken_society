const Coop = (function() {
    let activeFurniture = false; 

	const functions = {
		// Load all previously started quests. To be called when saveData is loaded.
		initialize() {

        },

        load() {

        },

        update(dt) {

        },

        draw() {

        },

        edit(itemName) {

        },

        placeFurniture(x, y) {

        },

        removeFurniture(x, y) {

        }
    };
	
 return functions; })()